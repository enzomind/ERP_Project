<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.choongang.erpproject.edms.mapper.Hr_Mapper">

    <sql id="insertColumns">
        lev_num
        , emp_id
        , appr
        , appr_state
        , appr_date
        , appl_date
        , lev_type
        , lev_title
        , lev_content
        , start_date
        , end_date
        , lev_term
    </sql>

    <select id="getLevList" resultType="Hr_ResponseDto">
        select lev_num, lev_title, appl_date, appr_state from lev_table
    </select>

</mapper>
